apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcpcoder.fullname" . }}
  labels:
    {{- include "mcpcoder.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcpcoder.selectorLabels" . | nindent 4 }}
